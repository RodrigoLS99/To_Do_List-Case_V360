<div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
  
  <div class="mb-6 flex items-center justify-between">
    <%= link_to todo_lists_path, class: "text-gray-500 hover:text-blue-600 flex items-center gap-1 text-sm font-medium transition duration-150" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
      Voltar para Minhas Listas
    <% end %>
  </div>

  <div class="bg-white rounded-xl shadow-lg border border-gray-100 overflow-hidden">
    
    <div class="px-8 py-6 border-b border-gray-50 bg-gray-50/50">
      <h1 class="text-2xl font-bold text-gray-900 flex items-center gap-2">
        ✏️ Editar Lista
      </h1>
      <p class="text-sm text-gray-500 mt-1">
        Altere o nome da sua lista de tarefas abaixo.
      </p>
    </div>

    <div class="p-8">
      <%= render "form", todo_list: @todo_list %>
    </div>

    <div class="bg-red-50 px-8 py-4 border-t border-red-100 flex flex-col sm:flex-row justify-between items-center gap-4">
      <div class="text-sm text-red-800">
        <strong>Zona de Perigo:</strong> Apagar esta lista removerá todas as tarefas nela.
      </div>
      
      <%= button_to @todo_list, method: :delete, form: { data: { turbo_confirm: "Tem certeza absoluta? Isso não pode ser desfeito." } }, class: "text-red-600 bg-white border border-red-200 hover:bg-red-600 hover:text-white font-bold py-2 px-4 rounded transition duration-200 text-sm cursor-pointer shadow-sm" do %>
        Excluir Lista Definitivamente
      <% end %>
    </div>
  
  </div>
</div>