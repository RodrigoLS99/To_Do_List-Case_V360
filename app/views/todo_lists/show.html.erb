<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  
  <div class="mb-6">
    <%= link_to todo_lists_path, class: "text-gray-500 hover:text-blue-600 flex items-center gap-1 text-sm font-medium transition" do %>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
      Voltar para Minhas Listas
    <% end %>
  </div>

  <div class="bg-white rounded-t-xl shadow-sm border border-gray-200 p-6 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4">
    
    <div>
      <h1 class="text-3xl font-bold text-gray-900 flex items-center gap-2">
        📝 <%= @todo_list.title %>
      </h1>
      <p class="text-sm text-gray-500 mt-1 ml-1">
        Criada em <%= @todo_list.created_at.strftime("%d/%m/%Y") %>
      </p>
    </div>

    <div class="flex items-center gap-2">
      <%= link_to edit_todo_list_path(@todo_list), class: "px-4 py-2 bg-white border border-gray-300 text-gray-700 font-medium rounded-lg hover:bg-gray-50 transition text-sm flex items-center gap-2" do %>
        <span>✏️</span> Editar
      <% end %>

      <%= button_to @todo_list, method: :delete, form: { data: { turbo_confirm: "Tem certeza que deseja apagar esta lista e todas as tarefas dela?" } }, class: "px-4 py-2 bg-red-50 border border-red-100 text-red-600 font-medium rounded-lg hover:bg-red-100 transition text-sm flex items-center gap-2" do %>
        <span>🗑️</span> Apagar
      <% end %>
    </div>
  </div>

  <div class="bg-gray-50 border-x border-b border-gray-200 rounded-b-xl p-8 min-h-[300px]">
    
    <div class="text-center py-10 opacity-50">
      <p class="text-4xl mb-2">👇</p>
      <p class="text-gray-600">Aqui entrarão os itens da lista...</p>
      <p class="text-sm text-gray-400">(Vamos criar isso agora na Task 6)</p>
    </div>

  </div>

</div>